import React from "react";
import { Grid } from "@material-ui/core";

const Blah: React.SFC = () => {
  return (
    <Grid container={true} spacing={16}>
      Hello
    </Grid>
  );
};

export default Blah;
